<form method="post" action="{{ path('app_programme_delete', {'idprogramme': programme.idprogramme}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme.idprogramme) }}">
    <button class="btn">Delete</button>
</form>
