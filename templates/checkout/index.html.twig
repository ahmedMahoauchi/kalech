{% extends 'base.html.twig' %}

{% block title %}Hello CheckoutController!{% endblock %}

{% block body %}
<div class="container">
    <main>      
      <div class="row g-5">
        <div class="col-md-5 col-lg-4 order-md-last">
          <h4 class="d-flex justify-content-between align-items-center mb-3">
            <span class="text-primary">Votre Panier</span>
            <span class="badge bg-primary rounded-pill">{{ items|length }}</span>
          </h4>
          <ul class="list-group mb-3">
            {% for item in items %}
            <li class="list-group-item d-flex justify-content-between lh-sm">
              <div>
                x{{item.quantite}}<h6 class="my-0">{{ item.product.nom }}</h6>
                <small class="text-muted">{{item.categorie.nomCategorie}}</small>
              </div>
              <span class="text-muted">{{item.product.prix}} Dt</span>
            </li>
            {% endfor %}
            
            <li class="list-group-item d-flex justify-content-between">
              <span>Total (Dt)</span>
              <strong>{{total}} Dt</strong>
            </li>
          </ul>
  
         
        </div>
        <div class="col-md-7 col-lg-8">
          <h4 class="mb-3">Vos coordonnées</h4>
          {{ form_start(form) }}
          
          {{ form_row(form.tel, {'attr': {'placeholder': 'Votre numero de telephone'}}) }}
          {{ form_row(form.email, {'attr': {'placeholder': 'Votre email'}}) }}

            <hr class="my-4">
  
  
            <button class="w-100 btn btn-primary btn-lg" type="submit">Continue to checkout</button>
            {{ form_end(form) }}
        </div>
      </div>
    </main>
</div>
  
{% endblock %}
